name: Apply Regime Patch (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. One-shot patch workflow is intentionally disabled to prevent repeated code rewrites."
