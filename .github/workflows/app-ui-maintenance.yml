name: App UI maintenance (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. UI patch workflow is intentionally disabled to prevent repeated automatic rewrites."
